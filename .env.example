# Environment Variables for YouTube Downloader Pro

# ==============================================
# REQUIRED FOR RENDER DEPLOYMENT
# ==============================================

# Node.js Environment
NODE_ENV=production

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://username:password@host:port/database_name

# Server Port (Render uses port 10000)
PORT=10000

# ==============================================
# DATABASE VARIABLES (Auto-provided by Render PostgreSQL)
# ==============================================

# These are automatically set when you create a PostgreSQL service on Render
PGHOST=your_postgres_host
PGPORT=5432
PGDATABASE=your_database_name
PGUSER=your_postgres_user
PGPASSWORD=your_postgres_password

# ==============================================
# OPTIONAL ENVIRONMENT VARIABLES
# ==============================================

# Session Secret (for user sessions if implemented)
SESSION_SECRET=your_random_secret_key_here

# Download Directory (optional, defaults to temp)
DOWNLOAD_DIR=/tmp/downloads

# Max File Size (optional, in bytes)
MAX_FILE_SIZE=1073741824

# Rate Limiting (optional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ==============================================
# INSTRUCTIONS FOR RENDER SETUP
# ==============================================

# 1. Create PostgreSQL Database on Render:
#    - Go to Render Dashboard
#    - Create New > PostgreSQL
#    - Copy the DATABASE_URL from the database info

# 2. Set Environment Variables in Web Service:
#    - NODE_ENV=production
#    - DATABASE_URL=(paste from PostgreSQL service)
#    - PORT=10000

# 3. Optional Variables (if needed):
#    - SESSION_SECRET=(generate random string)
#    - Any other custom variables your app uses

# ==============================================
# NOTES
# ==============================================

# - PORT is automatically set by Render, but you can override with 10000
# - DATABASE_URL is provided by Render PostgreSQL service
# - All PG* variables are auto-extracted from DATABASE_URL
# - Don't include sensitive values in version control